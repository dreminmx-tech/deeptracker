# 🔧 Технические детали

Как устроен deeptracker внутри: стек, данные, алгоритмы, PWA и тесты.
Для деплоя — [deploy.md](deploy.md).

<a id="stack"></a>

## 🧱 Стек

| Слой | Выбор | Почему так |
| --- | --- | --- |
| Сборка | **Vite 8** | мгновенный dev-сервер, статический `dist/` без рантайма |
| UI | **React 19** + TypeScript 7 (strict) | предсказуемое состояние, типы ловят опечатки в словарях |
| Состояние | React-контекст + `useState` | на таком объёме redux/zustand — лишняя зависимость |
| Хранение | `localStorage` | офлайн-first, ноль серверов, ноль аккаунтов |
| Офлайн | свой `public/sw.js` + манифест | без `vite-plugin-pwa`: меньше зависимостей, полный контроль |
| Тесты | Vitest (node-окружение) | логика в чистых модулях + `react-dom/server` для рендера |
| Иконки UI | **Lucide** (`lucide-react`) | 16 иконок, ~3 КБ gzip; своих рисованных глифов в интерфейсе нет |
| Иконки приложения | свой PNG-энкодер на `zlib` | никаких `sharp`/`canvas` в зависимостях |

<a id="structure"></a>

## 🗂 Структура

```
src/
  types.ts              типы данных: Habit, Entry, DayLog, AppData, Settings
  store.tsx             контекст стора, запись в localStorage на каждое изменение
  App.tsx               оболочка: вкладки (с deep-link по #hash), тема, язык, ролловер суток
  styles.css            обе темы: :root (тёмная) и [data-theme='light'] (чёрно-белая)
  lib/
    date.ts             локальные ключи дат YYYY-MM-DD, недели, Intl-форматирование
    habits.ts           чистая логика: статусы, мягкие серии, агрегаты, сводка по дням недели
    actions.ts          чистые мутации: тапы, счётчики, минуты, установка значения, CRUD привычек
    storage.ts          localStorage, нормализация, экспорт/импорт, скачивание файла
    i18n.ts             словари ru/en + стартовый набор привычек
    useInstallPrompt.ts хук установки PWA
    version.ts          версия приложения для настроек
  components/
    TodayView.tsx       экран «Сегодня»: любой день недели, инлайн-действия, брейн-дамп
    HabitsView.tsx      управление: порядок, закрепление, архив (через шторку)
    StatsView.tsx       полоса-график 30/90, итоги недели, дни недели, серии, проценты
    SettingsView.tsx    язык, тема, экспорт/импорт, установка, справка
    HabitRow.tsx        единственный паттерн строки: галочка, название, значение, кнопки
    CountStepper.tsx    «− значение +» в одной пилюле (счётчик и минуты)
    DayStrip.tsx        полоса недели: переход по дням и неделям
    TrendStrip.tsx      столбики по дням на всю ширину карточки
    ValueRow.tsx        строка сводки: тихая подпись слева, число справа
    LogDialog.tsx       быстрый ввод значений (чипы) и таймер — одно окно на оба случая
    habitText.ts        тексты строк: значение дня, подпись типа и расписания, шаги для чипов
    ActionSheet.tsx     шторка управления: иконка + текст
    HabitForm.tsx       создание и редактирование привычки
    Checkbox.tsx        квадратная галочка — главный тап-таргет
    Modal.tsx           шторка: заголовок, тело, футер из равных кнопок
    Toast.tsx           короткое сообщение; с действием — кнопка «Отменить»
scripts/
  make-icons.mjs        PNG-иконки и og.png (свой энкодер + растеризатор)
  shots.mjs             скриншоты через Chrome DevTools Protocol (docs/screenshots)
  smoke.mjs             проверка, что сервер и деплой отдают всю статику
```

Правило: всё в `src/lib/*` — чистые функции без React и DOM. Их тестируют напрямую, компоненты остаются тонкими.

<a id="ui"></a>

## 🎛 Интерфейс: правила

Система, а не набор удачных решений. Всё в `src/styles.css`:

| Что | Правило |
| --- | --- |
| Отступы | только `--s1…--s6` (4/8/12/16/24/32). Экран — `gap: var(--s5)` (`.stack`), карточка — `padding: var(--s4)` и `gap: var(--s3)`, подписанный блок внутри карточки — `--s2` (`.block`). Своих `margin` у блоков нет: ритм задаёт родитель |
| Высоты | **только контрольная шкала**: `--control-sm` (32 — сегменты и тихие пилюли), `--control`/`--tap` (44 — любая подписанная кнопка и любое поле ввода), `--icon` (40 — любая квадратная кнопка-иконка), `--row` (56 — строка списка). Раньше в файле жили 32/34/36/38/40/48/52/54 — отсюда и бралась «разномастность» |
| Типографика | только `--t-xs…--t-3xl` (12/13/15/17/22/32/48) и два веса: `--w-normal` (500) и `--w-strong` (650) |
| Форма | `--radius` (12) у карточек и тоста, `--radius-sm` (8) у **каждого** элемента управления: кнопки, сегменты, чипы, дни недели, прибор `− значение +`. Полностью круглыми остались только полосы (метр дня, столбик графика, отметка дня) — пилюля 999px рядом с прямоугольными кнопками читалась как чужой элемент |
| Карточка | один паттерн: заголовок, подпись, содержимое; 16px внутри, 12px между детьми, без «пустых» наполовину блоков |
| Строка | **один** компонент `HabitRow` на все списки: галочка, название, одно значение, кнопки. Правая часть строки — **две якорные колонки**: число (всегда заканчивается на общей вертикали) и действие (ровно `--icon` = 40px). Прибор `− значение +` занимает обе, поэтому его `+` стоит точно там, где у соседних строк иконка. Выравнивает ряды именно правый край, а не общая сетка: каждая строка — свой грид |
| Настройки | строки, а не плашки: `Язык [Русский|English]` — подпись слева, маленький переключатель справа. Полноширинный сегмент на бинарный выбор был самым тяжёлым элементом в приложении |
| Иерархия | что вообще имеет право быть громким — см. таблицу ниже |
| Опасное | отдельный токен `--danger` и только для разрушительных действий (удалить данные, удалить привычку). Ни одно состояние привычки красным не красится |

Что имеет право быть громким:

| Роль | Чем показывается |
| --- | --- |
| Состояние привычки | **единственный синий** `--accent`: отмеченная галочка, выбранный чип значения (сколько сделал сегодня), главная кнопка шторки. На большинстве экранов синего нет вовсе |
| «Ты здесь» | нейтральная приподнятая пилюля `--pick` (`#3a3d47`): активная вкладка, открытый день в полосе недели, выбранный диапазон 30/90, выбранные дни недели в форме. Это навигация и настройки, а не состояние, поэтому синего тут нет |
| История и цифры | нейтральная шкала `--cell-done` / `--cell-partial` / `--cell-missed`: полосы графиков, дни недели, кольцо дня, крупные числа. Синий в графике — **только сегодняшний столбик** |
| Границы | карточка читается тонкой светлой линией `--line` (`#33353c`), а не тяжёлой заливкой: `--surface-2` — всего один шаг над фоном. Сильная граница `--line-strong` — только у интерактивных элементов (кнопки, поля, пилюли) |
| Текст | два уровня: `--fg` для названий и всех значений, `--muted` для подписей. Подписи-заголовки — обычный регистр (`Не делать`, а не `НЕ ДЕЛАТЬ`): капс на каждом блоке читался как шум |

Экран «Сегодня»:

- Действия — **один прибор справа**, а не россыпь элементов. У счётчика и минут это
  `CountStepper`: `− значение +` внутри одной пилюли, где значение открывает быстрое окно.
  У «не делать» — `Ban` («сорвался»), у отметок и гибких — `Play` («просто начни», 2 минуты), причём
  `Play` исчезает у закрытой привычки: закрывать уже нечего, и строка остаётся с одной целью вместо двух.
  Три отдельных элемента в строке (чип, минус и плюс) читались как перегруз: это один прибор,
  поэтому у него одна рамка.
- **Значение — это кнопка.** `0/6` или `25/20 мин` открывает быстрое окно: сколько всего сделано
  сегодня. Внутри — чипы быстрых шагов (20 минут одним тапом), точная подстройка по единице тем же
  степпером и таймер. Раньше минус и плюс жили в строке, а «Записать 1 мин» в таймере приходилось
  нажимать двадцать раз.
- Тап по строке = выполнить. У «не делать» тапа по строке нет: срыв отмечается только явной кнопкой,
  чтобы случайное касание не портило статистику.
- Над списком — **полоса недели** (`DayStrip`): семь дней со статусом-отметкой и стрелки на неделю
  назад и вперёд. Выбранный день открывается в списке целиком, любое действие пишется в него.
  Так заполняется то, что забыл отметить вчера. Будущее недоступно: у кнопок `disabled`.
- Экран честно говорит, какой день открыт: заголовок меняется на «Прошлый день», рядом появляется
  «Вернуться к сегодня», а привычки, которых в тот день ещё не было, не показываются вовсе
  (`habitsUpTo`).
- **Одна спокойная фраза о дне** вместо уведомлений: «На сегодня всё. Дальше — отдых.» когда день
  закрыт, и «Вчера был пропуск. Один прощается — закрой сегодня, и серия цела.» когда есть риск
  (`missedYesterday`). Это та же механика «не пропускай дважды», но сказанная вовремя, а не в справке.
- **Действия, которые легко задеть случайно, оставляют путь назад**: тап по строке счётчика заполняет
  всю цель, поэтому тост предлагает «Отменить». То же для срыва, удаления мысли и выбора чипа в быстром
  окне. Все мутации чистые, поэтому отмена — это просто сохранённый `AppData` и `replace(before)`;
  отдельного журнала изменений не нужно. Тост с отменой живёт 5.2 с вместо 2.6 с — его надо успеть
  прочитать и нажать.
- Управление (изменить, порядок, закрепить, архив) живёт в шторке вкладки «Привычки»: там действия редкие,
  и им уместно иметь подписи — иконка + текст.
- Единицы измерения показываются только короткие (`мин`, `шт`); полный текст — в `title`.
- Числа везде с `font-variant-numeric: tabular-nums` — счётчики и таймер не дёргают вёрстку.
- Вкладки адресуются хэшем (`/#stats`), поэтому состояние открывается по ссылке и снимается скриптом.

### Быстрый ввод: «мне 20 раз нажимать?»

Счётчик и минуты нельзя заставлять набирать по единице. `LogDialog` отвечает на один вопрос —
«сколько всего за сегодня?» — рядом чипов (`quickSteps(target)`):

| Цель | Чипы |
| --- | --- |
| ≤ 10 | каждый шаг: `1 2 3 4 5 6` |
| 20 | `5 10 15 20` |
| 30 | `10 20 30` |
| 90 | `30 60 90` |

Выбор чипа пишет значение сразу (`setProgress`) и закрывает окно — один тап вместо двадцати. Степпер
в окне, наоборот, не закрывается: он для того, чтобы попасть в число точно. Ниже, в том же окне, живёт
таймер: `Старт`, `+2 мин`, `+5 мин` и «Записать N мин» (кнопка появляется только когда действительно
что-то отмерено, чтобы никто не записал случайную минуту). У «Простого старта» (2 минуты для
привычек-отметок) — то же окно в режиме `tiny`.

Статистика: вместо сетки 7×N — **полоса из столбиков на всю ширину карточки** (`TrendStrip`) с базовой
линией, высота = сколько закрыто за день: 100% — закрыт, 55% — частично, тонкая серая отметка — пусто,
ничего — привычки ещё не было. Заголовок («23 дня подряд») — такая же карточка, как остальные.

Разбивка по привычкам — **не карточки, а строки одного списка**: имя, серия, процент и своя полоска на
30/90 дней в каждой. Раньше каждая привычка была отдельной карточкой со своим заголовком, полосой и
строкой «серия N · лучшая N»: шесть привычек — это шесть почти одинаковых серых плит и стена столбиков
на весь экран. Теперь заголовок один на весь блок, «лучшая серия» осталась только в сводке сверху, а
список стал вчетверо короче и читается как таблица. Полоска внутри строки — тот же `TrendStrip` с
пропом `compact` (20px вместо 48px).

Вторая карточка — **по дням недели** (`weekdayRates`): доля закрытых привычек в каждый день недели за то
же окно 30/90. Считаются привычки, а не целые дни: день почти никогда не закрыт полностью, и «закрыто
40%» всё равно сигнал, а «закрыт 0 дней из 30» — уже нет. Под графиком одна строка вывода:
«Реже всего закрыто: чт.»

Третья — **итоги недели** (`weekReview`) сразу под заголовком: сколько дней из семи было с делами, что
закрывалось лучше всего, что хуже, и одна фраза вывода по тону недели. Это первое, что стоит прочитать
перед графиками. Здесь в счёт идут **все** привычки, включая гибкие и «не делать»: неделя — это и
«3 раза в неделю», и «ни разу не сорвался». Гибкая привычка меряется своей недельной целью, а не числом
дней, когда её делали, — иначе у неё всегда было бы 100%. Привычки моложе трёх ожидаемых дней в сводку
не попадают: по двум дням выводов не делают.

Иконки — **Lucide** (`lucide-react`), 16 штук, ~3 КБ gzip в бандле. Свои рисованные SVG убраны:
самодельные `Play`, `±`, таймер и текстовый `✕` выглядели кустарно.

Настройки — четыре карточки вместо шести, и на всей вкладке **нет ни одного синего** (у настроек нет
главного действия, значит и акцента нет):

- «Интерфейс» — две строки: подпись слева, маленький переключатель справа. Раньше это были две
  полноширинные плашки по 44px на бинарный выбор — самый тяжёлый элемент в приложении.
- «Данные» — две **одинаковые** кнопки (скачать / загрузить), строка состояния бэкапа, «Вставить JSON
  вручную» и справка «Как это работает» — обе свёрнуты в строку списка с шевроном (56px, обычный
  разделитель сверху), а не висят подчёркнутой ссылкой в пустоте.
- «Опасная зона» помечена **одним** красным — кнопкой. Красный заголовок над ней убран: три красных
  элемента подряд превращали редкое действие в тревогу.

Шрифт — **Manrope Variable** (`@fontsource-variable/manrope`), самохостинг с сабсетами cyrillic + latin:
39 КБ на первую загрузку, дальше из кэша service worker. Внешних запросов по-прежнему ноль.

<a id="data"></a>

## 💾 Модель данных

```ts
interface Habit {
  id: string;
  name: string;
  kind: 'check' | 'counter' | 'duration' | 'negative' | 'flex';
  target?: number;   // counter: цель в день, duration: целевые минуты
  unit?: string;     // «стаканов», «мин»
  perWeek?: number;  // flex: сколько раз в неделю
  step?: number;     // counter: шаг кнопок +/−
  days?: number[];   // дни недели, 0 = понедельник; нет поля — каждый день
  tiny?: string;     // версия привычки на 2 минуты для «просто начни»
  pinned?: boolean;  // показывать в «Главном сегодня» (максимум 3)
  archived?: boolean;
  createdAt: string; // ISO; дни раньше этой даты не считаются пропусками
  order: number;     // плотный порядок 0..n-1
}

interface Entry {
  value?: number;    // counter/duration — накопленное, negative — число срывов
  done?: boolean;    // check/flex — выполнено
  note?: string;
}

interface DayLog {
  entries: Record<string, Entry>; // ключ — id привычки
  dump: DumpItem[];               // брейн-дамп на этот день
}

interface AppData {
  version: number;
  habits: Habit[];
  days: Record<string, DayLog>;   // ключ — локальная дата YYYY-MM-DD
  settings: { lang: 'ru' | 'en'; theme: 'dark' | 'light' };
}
```

Принципы:

- **Дни адресуются локальной датой** (`YYYY-MM-DD` в таймзоне пользователя), а не UTC: «сегодня» должно совпадать с календарём на стене.
- **Пустое не хранится.** Снятие галочки удаляет запись, а день без записей и без мыслей удаляется целиком — база не пухнет от пустых объектов.
- **Хронология важна.** `createdAt` отделяет «привычки ещё не было» от «привычка пропущена»: до даты создания день имеет статус `rest` и не портит статистику.

<a id="logic"></a>

## 🧠 Логика привычек

### Правила выполнения

| Тип | Выполнено когда | Значение по умолчанию |
| --- | --- | --- |
| `check` | есть запись с `done: true` | не выполнено |
| `counter` | `value >= target` | не выполнено |
| `duration` | `value >= target` (минуты) | не выполнено |
| `negative` | `value === 0` — то есть срывов нет | **выполнено** (чисто по умолчанию) |
| `flex` | есть запись с `done: true` | `rest` — день ничего не требует |

### Статус дня

`dayStatus(data, habit, key)` возвращает одно из пяти значений:

| Статус | Когда |
| --- | --- |
| `future` | дата позже сегодня |
| `rest` | день раньше `createdAt`, день вне расписания, либо flex-привычка без записи |
| `done` | по правилам выше |
| `partial` | есть частичный прогресс: `0 < value < target` |
| `missed` | ничего не отмечено |

### Расписание

У привычки может быть список дней недели — `Habit.days`, `0` = понедельник. День вне списка становится
`rest`, а не пропуском: он не рвёт серию, не попадает в кольцо дня и в процент выполнения, а в
полосе-графике рисуется как «не требовалось». На экране «Сегодня» такая привычка не показывается —
вместо неё одна ссылка «Показать не по расписанию (1)», чтобы её всё-таки можно было отметить.

«Каждый день» хранится как **отсутствие поля**: пустой список и все семь дней нормализуются в `undefined`,
чтобы у самого частого случая не было состояния, которое можно рассогласовать. Гибкие привычки
(`flex`) живут по недельной цели и расписание игнорируют.

### Мягкие серии

Главная механика против срывов: один пропуск не обнуляет прогресс.

```
softStreak(habit):
  streak = 0; misses = 0
  идём назад от сегодняшнего дня до createdAt:
    rest / future            → пропускаем, ничего не меняем
    done                     → streak += 1; misses = 0
    missed / partial, i == 0 → сегодня ещё открыт, пропускаем
    missed / partial         → misses += 1; если misses >= 2 → стоп
  вернуть streak
```

То есть две пропущенные даты подряд ломают серию, одна — поглощается. `bestStreak()` прогоняет тот же автомат
по всей истории вперёд и запоминает максимум — так «лучшая серия» никогда не уменьшается.

### Агрегаты

| Функция | Что считает |
| --- | --- |
| `dayProgress` | кольцо дня: только «действенные» привычки (`check`, `counter`, `duration`) |
| `scheduledOn` | нужна ли привычка в этот день недели |
| `habitsUpTo` | привычки, которые уже существовали на эту дату: прошлое не требует того, чего ещё не было |
| `weekProgress` | прогресс flex-привычки за календарную неделю с понедельника |
| `completionRate` | доля выполненных дней за окно 30/90 дней |
| `habitCounts` | сколько раз привычка закрыта и сколько раз её ждали (для гибких — недельная цель) |
| `weekReview` | итоги недели: живых дней, лидер и отстающий |
| `weekdayRates` | доля закрытых привычек по дням недели: где именно проваливается |
| `missedYesterday` | сколько привычек пропущено вчера и всё ещё открыто сегодня |
| `dayHeatStatus` | статус всего дня для общей карты: `done` только если закрыты все |

`negative` и `flex` выведены из кольца дня намеренно: «не делать» не требует ежедневного действия,
а гибкая привычка измеряется неделей. Иначе прогресс показывал бы выполнение там, где ничего не делалось.

<a id="storage"></a>

## 🗄 Хранение и импорт

- Ключ в `localStorage`: **`deeptracker.v1`**, запись — на каждое изменение состояния (`store.tsx`).
- `loadData()` при отсутствии данных создаёт стартовый набор привычек на языке браузера.
- `normalizeData()` — защитный слой: незнакомый `kind` превращается в `check`, нечисловые цели отбрасываются, даты проверяются регуляркой, мусорные дни удаляются. Поэтому старый или руками правленный JSON не ломает приложение.
- `parseImport()` бросает ошибку на чужой JSON (например, без непустого массива `habits`) — интерфейс показывает понятный тост.
- `toJson()` / `downloadJson()` — бэкап файлом `deeptracker-YYYY-MM-DD.json`.
- **Дата последнего бэкапа** (`settings.lastExport`) пишется при экспорте и переживает экспорт/импорт. Данные живут только в этом браузере, поэтому экран настроек сам говорит, когда файл скачивали в последний раз, и напоминает, если прошло три недели или бэкапа не было вовсе. Это единственное напоминание в приложении, и оно появляется только когда есть что терять.

<a id="pwa"></a>

## 📴 PWA и офлайн

- `public/manifest.webmanifest` — standalone, `start_url: ./`, иконки `any` + `maskable`.
- `public/sw.js` — две стратегии:
  - **навигация** — network-first с фолбэком на кэш: свежий деплой подхватывается сразу, офлайн открывается последняя версия;
  - **остальное** — stale-while-revalidate: мгновенный ответ из кэша и тихое обновление в фоне.
- Service worker регистрируется **только в продакшн-сборке** и только по `http(s)` — с `file://` он невозможен, а в dev он мешал бы HMR.
- **Правило:** поменял оболочку (иконки, манифест, статику) — подними `CACHE_VERSION` в `public/sw.js`, иначе у установивших приложение останется старая закэшированная версия. Хэшированные ассеты и `index.html` обновляются сами.
- `public/.nojekyll` — чтобы GitHub Pages не пытался обрабатывать файлы через Jekyll.

<a id="palette"></a>

## 🎨 Палитра

Оба набора токенов лежат в `src/styles.css`: `:root` — тёмная тема (цвета сняты с дашборда DeepSeek Platform),
`[data-theme='light']` — строго чёрно-белая. Имена токенов одинаковые, поэтому все правила написаны один раз.

| Токен | Тёмная | Где используется |
| --- | --- | --- |
| `--bg` | `#131317` | фон страницы |
| `--surface` | `#1b1c20` | поля ввода, степпер, нижняя навигация — врезанные «колодцы» |
| `--surface-2` | `#23242a` | карточки и сгруппированные списки — один тихий шаг над фоном |
| `--surface-3` | `#2e303a` | наведение, активная кнопка-иконка |
| `--line` / `--line-strong` | `#33353c` / `#4a4d56` | тонкая граница карточки и разделитель строк / сильная граница интерактивных элементов |
| `--fg` / `--muted` | `#f7f8fc` / `#a4a8b4` | названия и все значения / подписи |
| `--pick` / `--pick-ink` | `#3a3d47` / `#f7f8fc` | «ты здесь»: активная вкладка, открытый день, диапазон, дни недели в форме |
| `--well` | `#1b1c20` | врезанный прибор на карточке — `− значение +`; рамки у него нет, форму держит сама заливка |
| `--accent` / `--accent-ink` | `#0070f3` / `#ffffff` | **единственный синий**: отмеченная галочка, выбранный чип значения, главная кнопка, фокус, сегодняшний столбик |
| `--good` | `#22c55e` | успешный импорт |
| `--cell-done` / `--cell-partial` / `--cell-missed` / `--cell-line` | `#b4b9c5` / `#6b7280` / `#3d4049` / `#5a5e66` | нейтральная шкала графиков: сделано / частично / пусто / «не требовалось» (пунктир) |
| `--danger` / `--danger-line` / `--danger-soft` | `#f2555a` / `#7c3437` / `rgba(242,85,90,.14)` | только разрушительные действия |

Жёлтого нет: «частично» — просто тусклее серый (`--cell-partial`), «пусто» — тонкая отметка у основания.
Красный — единственный дополнительный оттенок, и он закреплён за опасными действиями («Удалить все
данные»), а не за состояниями привычек: пустая клетка не оценка. Историю рисует нейтральная шкала, потому
что цвет в этом приложении означает состояние, а не давность: синим может быть только сегодняшний столбик.
Контраст проверен по WCAG: текст ≥ 4.5:1 на своих фонах, графические элементы ≥ 1.5:1. Столбики держат
отдельные токены, потому что лежат на серой карточке, а не на фоне страницы.

<a id="tests"></a>

## 🧪 Тесты

```bash
npm test          # 52 теста, ~0.5 c
npm run typecheck # tsc --noEmit отдельно
```

| Файл | Что проверяет |
| --- | --- |
| `src/lib/habits.test.ts` | правила тапа по всем пяти типам, статусы `rest`/`future`, расписание (день вне списка не рвёт серию и не попадает в кольцо дня), мягкие серии (одна пропущенная дата поглощается, две ломают, сегодня не наказывается), `bestStreak`, клампы счётчика, накопление минут, установку значения одним действием и шаги чипов `quickSteps`, дни до создания привычки, `missedYesterday`, сводку по дням недели, итоги недели с гибкими привычками и порогом «слишком свежая», сохранность даты бэкапа в экспорте, кольцо дня, лимит трёх закреплённых, перестановка порядка, удаление привычки вместе с историей, экспорт/импорт, нормализация мусорного JSON |
| `src/app.test.tsx` | рендер оболочки и всех экранов через `react-dom/server` с подставленным `localStorage`: ловит падения на реальных данных, включая пустое состояние статистики, быстрое окно ввода (4 чипа для цели 20 минут), заполнение прошлого дня через полосу недели, фразы о состоянии дня, скрытие привычек не по расписанию, напоминание о бэкапе и английскую локаль |

Тесты детерминированы: даты считаются от локального «сегодня», идентификаторы привычек фиксированы,
`localStorage` подменяется заглушкой.

<a id="scripts"></a>

## 🛠 Скрипты

| Команда | Что делает |
| --- | --- |
| `npm run dev` | dev-сервер на `127.0.0.1:5173` (явный IPv4 — см. [траблшутинг](deploy.md#troubleshooting)) |
| `npm run build` | `tsc --noEmit` + сборка в `dist/` |
| `npm run preview` | статический сервер продакшн-сборки на `127.0.0.1:4173` |
| `npm test` | прогон Vitest |
| `npm run typecheck` | только типы |
| `npm run icons` | перегенерировать `public/icons/*` и `public/og.png` |
| `npm run shots` | снять скриншоты всех экранов в `docs/screenshots` (нужен запущенный `npm run preview`) |

`scripts/make-icons.mjs` не тянет графических библиотек: PNG собирается вручную (IHDR/IDAT/IEND + CRC32
и `zlib.deflateSync`), а галочка рисуется суперсэмплингом 4×4 с премультиплицированным усреднением —
поэтому скруглённые углы и концы штриха получаются гладкими.

`scripts/shots.mjs` управляет headless Chrome по DevTools Protocol: подставляет демо-данные за 24 дня
init-скриптом (до гидратации приложения — иначе гонка), открывает вкладки по хэшу и сохраняет PNG.
Путь к браузеру — `CHROME_PATH`, адрес — `SHOTS_BASE`.

`scripts/smoke.mjs` проверяет, что сервер или деплой реально отдаёт всю статику и SEO-разметку:

```bash
node scripts/smoke.mjs                            # dev на :5173
node scripts/smoke.mjs http://127.0.0.1:4173/     # локальный preview
node scripts/smoke.mjs https://<user>.github.io/<repo>/   # живой деплой
```

Скрипт сам определяет режим (dev или продакшн-сборка), качает `index.html`, точку входа или хэшированный
бандл, манифест, service worker, favicon, иконки, `robots.txt`, `sitemap.xml`, `og.png`, проверяет
`<title>`, `description`, `canonical`, Open Graph, Twitter-card и JSON-LD, и падает с ненулевым кодом,
если чего-то не хватает.

<a id="build"></a>

## 📦 Сборка

| Параметр | Значение |
| --- | --- |
| `base` | `'./'` — относительные пути, один и тот же `dist/` работает на project- и user-страницах Pages и на своём домене |
| `target` | `es2019` |
| JS | ~291 kB raw / **~90 kB gzip** (включая ~3 КБ иконок Lucide) |
| CSS | ~24 kB raw / **~7.6 kB gzip** |
| Шрифт | `assets/manrope-*-wght-normal.woff2`, 5 сабсетов ~72 КБ в сборке; браузер скачивает только cyrillic + latin (~39 КБ) |
| Артефакты | `index.html`, `assets/*`, плюс всё из `public/`: манифест, `sw.js`, `robots.txt`, `sitemap.xml`, `og.png`, иконки |

Внешних CDN и запросов наружу нет — шрифт самохостится из бандла, поэтому приложение работает офлайн.

<a id="i18n"></a>

## 🌍 Локализация

Словари в `src/lib/i18n.ts`. `ru` — источник истины: тип `Dict` выводится из него, поэтому забытый или
опечатанный ключ в `en` ломает `npm run typecheck`, а не доезжает до продакшена. Подстановки — через `fill()`
(`{done} из {total}`). Стартовый набор привычек тоже локализован (`SEED_HABITS`).

Добавить язык: скопировать объект `en`, перевести, добавить в `STRINGS` и в тип `Lang`.
